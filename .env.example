# Token of the bot registered with @BotFather; must have Business mode enabled.
BOT_TOKEN=

# SQLite (default, fine for local dev):
DATABASE_URL=sqlite+aiosqlite:///./bot.db
# PostgreSQL (used by docker-compose):
# DATABASE_URL=postgresql+asyncpg://bot:bot@db:5432/bot

LOG_LEVEL=INFO
